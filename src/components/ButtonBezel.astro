---
const { href = "", size = "full", add = "" } = Astro.props;
const Type = href === "" ? "button" : "a";
const Size =
  size === "s"
    ? "w-24"
    : size === "m"
      ? "w-48"
      : size === "l"
        ? "w-72"
        : size === "full"
          ? "w-full"
          : "";

const background =
  "bg-gradient-to-t from-[#00eddf] to-[#00bfb9] w- text-[#003b3d] ";
const hoverBackround =
  "hover:bg-gradient-to-t hover:from-[#00bfb9] hover:to-[#009794]";
const activeBackround =
  "active:bg-gradient-to-t  active:from-[#009794] active:to-[#007777] disabled:to-[#003b3d] disabled:text-[#009794]";
---

<Type
  class={`🦖 p-2 rounded-lg bg-gradient ${background} ${hoverBackround}  ${activeBackround}  ${add} ${Size}`}
>
  <slot />
</Type>

<style>
  .🦖 {
    box-shadow: #007777 0px 2px 0px 0px;
  }
</style>
